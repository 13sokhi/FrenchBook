<Window x:Class="FrenchBookApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrenchBookApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <!--
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>        
            <RowDefinition Height="*"/>          
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="My French Topics" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="10" HorizontalAlignment="Center" Grid.Row="0"/>

        <ScrollViewer Grid.Row="1" Margin="40" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="TopicsPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBox x:Name="NewTopicTextBox" Width="350" Height="30" FontSize="16" Padding="5" Margin="10" VerticalContentAlignment="Center"/>

            <Button Content="Add" Width="80" Height="30" Margin="10,0,0,0" Background="#007ACC" Foreground="White" Click="AddTopic_Click"/>
        </StackPanel
                    -->
    </Grid>
</Window>
